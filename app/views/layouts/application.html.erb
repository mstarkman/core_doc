<!DOCTYPE html>
<html>
<head>
  <title>CoreDoc</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container" ng-app="CoreDoc">
  <div ng-controller="CoreDocCtrl">
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <a class="brand" href="/">CoreDoc</a>
        <div class="alert alert-info alert-message span2" ng-show="isLoading">
          Loading...
        </div>
        <div class="alert alert-info alert-message span2" ng-show="isShowingFlash">
          {{flashMessage}}
        </div>
        <div class="alert alert-info alert-message span2" ng-show="isSaving">
          {{saveMessage}}
        </div>
      </div>
    </div>
    <%= yield %>
  </div>
</div>

</body>
</html>
